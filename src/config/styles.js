import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex:1,
    },

    botao: {
        padding: 5,
        borderRadius: 5,
        backgroundColor: "#87CEFA",
        fontSize: 20,
    },

    barra: {
        margin: 10
    },

    text:{
        marginHorizontal: "5%",
    }
})